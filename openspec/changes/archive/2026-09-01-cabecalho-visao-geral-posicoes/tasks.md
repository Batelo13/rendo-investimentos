## Tasks

- [x] Adicionar linha `.mini-row.mini-row-header` estática em `dashboard.html`, antes de `#dashPosicoes`, com os rótulos Ativo/Corretora/Qtd/Preço médio/Preço atual/Resultado/Valor.
- [x] Estilizar `.mini-row-header` em `dashboard.css` (texto maiúsculo, pequeno, cor secundária — mesmo padrão de `.table th`).
- [x] Replicar as regras de ocultação responsiva das colunas (`display:none` nos 2 breakpoints existentes) para as células correspondentes do cabeçalho.
- [x] Verificar em navegador real: cabeçalho alinhado com uma linha de dados real (comprado PETR4), rótulo "Preço médio" sem "acumulado".
- [x] Rodar suite completa (`mvnw test`) — mudança é 100% frontend, sem impacto esperado.
